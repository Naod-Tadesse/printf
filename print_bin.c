#include "main.h"

/**
 * print_bin - prints binary representation of decimal
 * @arguments: the number to be converted
 *
 */

int print_bin(va_list arguments)
{
	int num = va_arg(arguments, unsigned int);
	int binNum[32];
}
